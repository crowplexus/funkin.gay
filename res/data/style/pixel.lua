local PixelHUD = require("game.scripting.hudskin")

function PixelHUD:onHUDLoaded()
end

function PixelHUD:onReceptorLoaded(receptor)
end

function PixelHUD:onNoteLoaded(note)
end

function PixelHUD:onSplashLoaded(splash)
    return nil
end

return PixelHUD -- Don't forget this
